#include "Structs.h"
#include <stdio.h>
#include <stdlib.h>
#ifndef INPUT_H_
#define INPUT_H_

data* inputPN(FILE *fin);

void calcList(list l1, list l2);

void calcQueue(queue q1, queue q2);
#endif /* INPUT_H_ */
